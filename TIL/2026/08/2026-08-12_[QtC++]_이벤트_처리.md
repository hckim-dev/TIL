# \[Qt/C++\] 이벤트 처리

> 날짜: 2026-08-12
> 원본 노션: [링크](https://app.notion.com/p/Qt-C-3ba1d46c18fc80cea34ffec7f320f51e)

<!-- notion-page-id: 3ba1d46c18fc80cea34ffec7f320f51e -->
<!-- notion-title: "[Qt/C++] 이벤트 처리" -->

---

<a id="notion-3ba1d46c18fc80b89fb0f1572eb033c7"></a>

# ⚡ Qt Event Handler (이벤트 처리기) 정리

> **💡 한 줄 요약**
>
> **Event Handler**는 키보드/마우스 등의 입력 이벤트가 발생했을 때 실행되는 함수이며, `QWidget`에 정의된 \*\*가상 함수(`virtual`)를 재정의(Override)\*\*하여 원하는 동작을 직접 구현할 수 있습니다.

<a id="notion-3ba1d46c18fc80438a43d57730e0bc2b"></a>

## 1\. 핵심 개념

- **Event Handler란?**

  - 사용자나 시스템의 이벤트(키보드 누름, 마우스 클릭, 창 크기 변경 등)를 가로채서 처리하는 멤버 함수입니다.
- <strong>`QWidget`</strong>**과의 관계**

  - `QWidget` 클래스 내부에는 이미 다양한 이벤트 핸들러가 가상 함수(`virtual`)로 구현되어 있습니다.
- **재정의 (Override)**

  - 자식 클래스(예: `MainWindow`)에서 부모의 이벤트 핸들러를 재정의(Override)하면 기본 이벤트 처리 방식을 내 입맛대로 변경할 수 있습니다.

<a id="notion-3ba1d46c18fc800fbd4af9f4ebada170"></a>

## 2\. 주요 Event Handler 목록

|  |  |  |  |
| --- | --- | --- | --- |
| **이벤트 종류** | **가상 함수 (Event Handler)** | **인자 (Argument)** | **설명** |
| **키보드 누름** | `keyPressEvent()` | `QKeyEvent*` | 키보드 키가 눌렸을 때 호출 |
| **키보드 뗌** | `keyReleaseEvent()` | `QKeyEvent*` | 키보드 키에서 손을 뗐을 때 호출 |
| **마우스 클릭** | `mousePressEvent()` | `QMouseEvent*` | 마우스 버튼을 눌렀을 때 호출 |
| **마우스 이동** | `mouseMoveEvent()` | `QMouseEvent*` | 마우스를 움직일 때 호출 |
| **창 크기 변경** | `resizeEvent()` | `QResizeEvent*` | 위젯의 크기가 변경될 때 호출 |

<a id="notion-3ba1d46c18fc80439861c8fc0a703669"></a>

## 3\. 예시 코드 (C++ Qt)

<a id="notion-3ba1d46c18fc802d882fd979036ec607"></a>

### 📄 `mainwindow.h` (헤더 파일)

```cpp
#ifndef MAINWINDOW_H
#define MAINWINDOW_H

#include <QMainWindow>
#include <QKeyEvent>   // 키보드 이벤트 헤더
#include <QMouseEvent> // 마우스 이벤트 헤더

class MainWindow : public QMainWindow {
    Q_OBJECT

public:
    MainWindow(QWidget *parent = nullptr);

protected:
    // 💡 부모(QWidget)의 가상 함수를 override 선언
    void keyPressEvent(QKeyEvent *event) override;
    void mousePressEvent(QMouseEvent *event) override;
};

#endif // MAINWINDOW_H
```

<a id="notion-3ba1d46c18fc80f3aa81d4dc19b040e4"></a>

### 📄 `mainwindow.cpp` (소스 파일)

```cpp
#include "mainwindow.h"
#include <QDebug>

MainWindow::MainWindow(QWidget *parent) : QMainWindow(parent) {}

// 1. 키보드 이벤트 재정의
void MainWindow::keyPressEvent(QKeyEvent *event){
    if (event->key() == Qt::Key_Escape) {
        qDebug() << "Esc 키가 눌려 프로그램을 종료합니다.";
        this->close();
    } else {
        // ⚠️ 내가 처리하지 않은 다른 키는 부모의 기본 이벤트 핸들러로 전달
        QMainWindow::keyPressEvent(event);
    }
}

// 2. 마우스 이벤트 재정의
void MainWindow::mousePressEvent(QMouseEvent *event){
    if (event->button() == Qt::LeftButton) {
        // 마우스 좌클릭 시 클릭한 좌표 출력
        qDebug() << "마우스 좌클릭 위치 (X, Y):" << event->pos();
    }
}
```

<a id="notion-3ba1d46c18fc80df98ccfb4814f6db66"></a>

## 4\. 📌 이벤트 재정의 시 유의사항

1. **`override`** <strong>키워드 붙이기</strong>

   - 오타로 인해 상속이 안 되는 불상사를 막기 위해 헤더 선언 시 `override`를 붙이는 것이 안전합니다.
2. **부모 클래스 함수 호출하기 (기본 동작 유지)**

   - 조건문에 해당하지 않는 키/마우스 입력은 `QMainWindow::keyPressEvent(event);`처럼 부모 함수를 호출해 주어야 기존 Qt의 기본 동작(탭 이동, 기본 포커스 제어 등)이 깨지지 않습니다.

<a id="notion-3ba1d46c18fc8011b1d9d682ba15d6d7"></a>

## 5\. Custom Button

```cpp
#ifndef MYBUTTON_H
#define MYBUTTON_H

#include <QDebug>
#include <QMouseEvent> // include
#include <QPushButton>

class MyButton : public QPushButton {
    Q_OBJECT

public:
    explicit MyButton(QWidget* parent = nullptr);
    int getClickCount();

protected:
    void mousePressEvent(QMouseEvent* event) override; // override 써주기
    void mouseReleaseEvent(QMouseEvent* event) override;

private:
    int clickCount;
};

#endif // MYBUTTON_H
```

```cpp
#include "mybutton.h"

MyButton::MyButton(QWidget* parent)
    : QPushButton(parent)
{
    clickCount = 0;

    setStyleSheet("background-color: yellow;"); // 본인 속성 바꾸는 것이므로 바로 함수 호출
    qDebug() << "MyButton created";
}

int MyButton::getClickCount()
{
    return clickCount;
}

void MyButton::mousePressEvent(QMouseEvent* event)
{
    clickCount++;

    if (event->button() == Qt::LeftButton) {
        setStyleSheet("background-color: red;");
        qDebug() << "mousePressEvent";
    }

    QPushButton::mousePressEvent(event); // 부모 이벤트 호출
}

void MyButton::mouseReleaseEvent(QMouseEvent* event)
{
    if (event->button() == Qt::LeftButton) {
        setStyleSheet("background-color: green;");
        qDebug() << "mouseReleaseEvent";
    }

    QPushButton::mouseReleaseEvent(event);
}
```
